\section{Week 3: Tokenization and Embeddings — From Text to Vectors}
\label{sec:week3-tokemb}

\subsection*{Setting the Scene}
\addcontentsline{toc}{subsection}{Setting the Scene}

\paragraph{The problem.} Chapter~1 spoke of probabilities over
tokens as if a token were an obvious thing. It is not. A computer
sees only numbers; a language has, in any reasonable accounting,
an unbounded number of words, and even an unbounded number of
\emph{ways} to write any given word. Before any model can predict
the next token, somebody has to decide what counts as a token. The
decision sounds technical and turns out to be deeply
consequential. Tokenization is the model's alphabet. Whatever the
model cannot see at the alphabet level, it cannot see at all.

\paragraph{How we got here.} The earliest computational
linguistics treated the word as the unit of analysis. This was
intuitive --- a dictionary is a list of words --- and almost
immediately catastrophic. English has too many of them.
Inflectional languages (Finnish, Turkish, Hungarian) have far too
many. Compound-forming languages (German, Dutch, Japanese) have
unboundedly many. Every model encountered, in production, words
that were not in its vocabulary. We called these
\emph{out-of-vocabulary} tokens, and we replaced them with a
single \texttt{<unk>} symbol that destroyed any information they
carried. A model whose answer to ``what is X?'' is always
``unknown'' is not a useful model.

The character-level alternative, briefly fashionable in the
mid-2010s, replaced the word with the letter. This eliminated the
out-of-vocabulary problem at the cost of forcing the model to
learn long-range dependencies the hard way. Each English word
turned into five or six tokens; each context window had to be
five or six times as long; the gradient signal grew correspondingly
weaker. There were beautiful character-level models that could,
just, work. There were no widely-deployed ones.

The compromise that won was the \emph{subword}: a unit smaller than
a word but larger than a letter, learned from the corpus rather
than handed down by a linguist. Rico Sennrich, Barry Haddow, and
Alexandra Birch, in 2016, adapted Philip Gage's 1994 byte-pair
encoding (originally a data-compression algorithm) to neural
machine translation. They showed that a vocabulary of around
$32{,}000$ subword units could represent any string in any
language, deterministically, with no out-of-vocabulary tokens at
all. Mike Schuster and Kaisuke Nakajima, working on Google's
voice search, had described a related approach called WordPiece a
few years earlier; BERT made it famous in 2018. Taku Kudo at
Google built a third, the Unigram language-model tokenizer, with
its companion library SentencePiece in 2018, which used
whitespace as a sentinel and so handled languages without space
delimiters cleanly.

By 2020 every modern language model was using one of these three
algorithms or a small variant. By 2024 the variants had
converged: byte-level BPE for English-heavy and code-heavy
models, SentencePiece-Unigram for multilingual ones. The
tokenizer is now a frozen artefact, trained once before
pretraining begins and never modified, because changing it
invalidates every token-ID the model has ever seen.

\paragraph{Where we stand.} A modern tokenizer is small,
deterministic, lossless, and finite. Given a string, it produces
a sequence of integers. Given that sequence, it reconstructs the
string. It does this without consulting a dictionary, which means
it works for words it has never seen, including invented words,
typos, and code identifiers. The mathematics in the rest of this
chapter unpacks why the WordPiece score is the unigram
log-likelihood gain of a merge, why the Unigram-LM tokenizer is
trained by EM with a pruning step, and why the embedding layer
that follows tokenization is a one-hot lookup masquerading as a
matrix multiplication. The geometry of the resulting embedding
space is not arbitrary. It encodes, in its directions and
distances, statistical regularities of the training corpus. That
is what we mean when we say the model ``learns the meaning of a
word.''

\paragraph{What goes wrong.} Tokenization is a quiet source of
several user-visible problems. The most consequential is the
multilingual disparity: the same sentence in Thai, Amharic, or
Burmese costs typically two to five times as many tokens as the
English original under a tokenizer trained on English-heavy data.
This means a 32k-token context window covers far less Thai than
English, and a per-token-priced API charges far more. The fairness
implication is clear, and several frontier-model providers now
publish multilingual-tuned tokenizers as a partial response.

A second problem is brittleness at the token boundary. Two prompts
a human would read as identical can segment differently if a
whitespace is added, removed, or shifted. The model has learned
the statistics of the segmentation it was trained on; the
slightly different segmentation produced by a careless
concatenation can shift the output enough to matter. Engineers
who paste prompts together at runtime, without testing the
resulting tokenization, sometimes ship products that misbehave in
ways no unit test caught.

A third problem is the ``the model doesn't know X'' phenomenon.
The model often does know X. The token sequence that names X is
just rare or split awkwardly across token boundaries, and the
model's grip on it is correspondingly weak. Calling this an
ignorance failure obscures the underlying tokenization issue.

A fourth problem is adversarial. Unicode is permissive about
homoglyphs (characters that look identical, like Latin and
Cyrillic ``a''), about invisible code points, and about composed
versus decomposed forms. An attacker can use these to write a
prompt that, after tokenization, asks the model to do something
the visible text does not. The mitigation is normalisation at
ingress and logging at every layer; the math in this chapter does
not solve the problem, but the engineering vocabulary of token
boundaries is the vocabulary in which the problem is named.

\subsection{Learning Objectives}

By the end of this week, you should be able to:
\begin{itemize}
  \item Walk through the Byte-Pair Encoding (BPE) algorithm end-to-end
        and trace how a concrete string becomes a token sequence.
  \item Contrast BPE, WordPiece, and Unigram-LM tokenizers on
        objective, determinism, reversibility, and failure modes.
  \item Explain why byte-level BPE defeats the Unicode-normalization
        problems that plague word- and character-level models.
  \item Derive the embedding layer mathematically as a one-hot lookup
        and justify tied input/output embeddings on a memory and
        regularization basis.
  \item Describe the distributional hypothesis, explain why analogy
        vectors sometimes work, and reason about the geometry
        (clusters, anisotropy, PCA/t-SNE) of learned embeddings.
  \item Identify bias and fairness risks carried into embeddings from
        training data, and articulate engineering mitigations.
  \item Design a tokenizer-versioning policy, including multilingual
        token-budget planning and vocabulary-drift detection.
\end{itemize}

\subsection{LLM Components Covered}

\textbf{Input representation}
\begin{itemize}
  \item Unicode normalization (NFC / NFD / NFKC).
  \item Pre-tokenization (whitespace, punctuation, byte-fallback).
  \item Vocabulary construction (BPE, WordPiece, Unigram, byte-level).
  \item Special tokens (\texttt{<|bos|>}, \texttt{<|pad|>},
        \texttt{<|eos|>}, chat templates).
\end{itemize}

\textbf{Representation learning}
\begin{itemize}
  \item Embedding layer as a linear map from one-hot to $\mathbb{R}^d$.
  \item Tied embeddings between the input table and the output softmax.
  \item Positional encoding as an additive representation.
\end{itemize}

\textbf{Systems relevance}
\begin{itemize}
  \item Tokenizer as a versioned model artifact.
  \item Token-count billing and multilingual fairness.
  \item Prompt brittleness at token boundaries.
\end{itemize}

\subsection{Notation and Standing Assumptions}
\label{sec:week3-notation}

\begin{notationbox}
\textbf{Alphabet and token set.}
$\Sigma$: Unicode-codepoint or UTF-8 byte alphabet ($|\Sigma| = 256$
for byte-level). $\mathcal{V}$: the trained vocabulary of tokens,
with $|\mathcal{V}| = V$ (typical $V \in [32{,}000, 256{,}000]$).
Token identifiers are integers $\iota \in \{0, 1, \dots, V-1\}$. We
reserve $i$ for positions in a token \emph{sequence} (the $i$-th
token in context) and $\iota$ for vocabulary-item indices (the
$\iota$-th row of $E$). Where context is unambiguous, we write the
one-hot indicator $\mathbf{1}_\iota \in \{0,1\}^V$.

\textbf{Sequences.}
Input text of length $T$ tokens produces
$\mathbf{x} = (x_1, \dots, x_T)$ with $x_i \in \mathcal{V}$, or
equivalently a sequence of IDs $(\iota_1, \dots, \iota_T)$.
Embedding matrix $E \in \R^{V \times d}$, with $d$ the model width.

\textbf{Pre-processing.}
$\mathrm{NF}(\cdot)$: a Unicode normalization form
(NFC/NFD/NFKC/NFKD). Pre-tokenization $\pi_{\mathrm{pre}}$ splits
strings into candidate units (whitespace-delimited words, byte
runs). The full tokenizer pipeline is
$\mathcal{T} = \mathrm{enc}\circ\pi_{\mathrm{pre}}\circ\mathrm{NF}$.

\textbf{Standing assumptions.}
\textbf{(A3.1)} \emph{Tokenizer determinism.} Given fixed
$\mathrm{NF}$, $\pi_{\mathrm{pre}}$, and merge rules (or Unigram-LM
parameters), $\mathcal{T}$ is a deterministic function. Encoding
drift between training and inference --- different normalization or
pre-tokenization --- breaks this and yields out-of-distribution IDs.
\textbf{(A3.2)} \emph{Lossless detokenization} is guaranteed by
byte-level BPE and SentencePiece (which treat whitespace as a
sentinel), and is \emph{not} guaranteed by classical BPE or
WordPiece, which require language-specific detokenization rules.
\textbf{(A3.3)} Anisotropy ranges, multilingual token-inflation
factors, and analogy-recovery rates are \Emp\ empirical observations
on specific corpora and models; they must be re-measured on the
deployed tokenizer and distribution, not assumed.
\end{notationbox}

\subsection{Conceptual Core: Discrete Symbols to Vectors}

Neural networks operate on $\mathbb{R}^d$; natural language is discrete,
hierarchical, and Unicode-encoded. Every LLM pipeline begins with a
deterministic function
\begin{equation}
\label{eq:week3-pipeline}
\text{text} \;\xrightarrow{\text{normalize}}\;
\text{code-points} \;\xrightarrow{\text{tokenize}}\;
\text{token sequence} \;\xrightarrow{\text{vocab}}\;
\text{integer IDs} \;\xrightarrow{\text{embed}}\;
\mathbb{R}^{T \times d}.
\end{equation}
See Figure~\ref{fig:week3-pipeline}. After embedding, the rest of the
Transformer (Week~4) operates purely on floats; the model never sees a
character or a Unicode code-point again. The tokenizer is therefore a
\emph{model boundary}: it defines the alphabet the rest of the system
will ever perceive, and it must match exactly between training and
inference, otherwise the model is effectively reading garbled input.

\begin{figure}[h]
\centering
\input{figures/tokenization_pipeline}
\caption{The tokenization pipeline: text is normalized, pre-tokenized,
  mapped to integer IDs via the vocabulary, and projected into
  $\mathbb{R}^d$ through the embedding matrix $E$.
  After this step the model operates purely on dense vectors.}
\label{fig:week3-pipeline}
\end{figure}

Three engineering claims follow immediately:
\begin{itemize}
  \item \textbf{The tokenizer is part of the model}. Changing it
        silently changes semantics; it must be version-pinned alongside
        the weights.
  \item \textbf{Tokenization is lossy}. Information absent from the
        token sequence is unrecoverable downstream (e.g., a
        capitalization lost in lowercasing).
  \item \textbf{Tokenization determines the prior}. Tokens are the
        atomic events in the cross-entropy of Week~1; changing them
        changes both the likelihood surface and the notion of
        perplexity.
\end{itemize}

\subsection{Unicode, Bytes, and Normalization}

Natural-language text in modern systems is Unicode code-points, not
bytes. The same visual string can have multiple code-point encodings
(the ``\'e'' can be one code-point U+00E9 or two code-points
U+0065~U+0301), and these are \emph{not equal} as integer sequences.
Normalization forms resolve this:
\begin{itemize}
  \item \textbf{NFC} (composed) — merges combining marks into
        precomposed code-points; preferred for storage.
  \item \textbf{NFD} (decomposed) — splits precomposed code-points;
        preferred for collation.
  \item \textbf{NFKC / NFKD} — additionally merge compatibility
        variants (e.g., full-width Latin to ASCII).
\end{itemize}
A tokenizer trained on NFC text but applied to NFD input will segment
the input differently, and the model will see out-of-distribution IDs.
This is a real production bug: production prompts from Windows clipboard
paste sometimes arrive in a different normalization form than the
training corpus.

Byte-level tokenizers sidestep part of this problem by working on UTF-8
byte sequences ($\mathbb{B} = \{0, \ldots, 255\}$), which is canonical
by construction. The tradeoff is that naive byte tokenization inflates
token counts by roughly $3\times$ for many scripts; byte-level BPE
recovers much of this by \emph{merging} byte sequences into higher-level
tokens while retaining the byte alphabet as a fallback.

\subsection{Word-Level Tokenization and Its Failure Modes}

Word-level tokenization splits on whitespace and punctuation. It is
intuitive but fails at scale for four reasons.

\begin{enumerate}
  \item \textbf{Out-of-vocabulary (OOV) tokens.} Any word not in the
        training vocabulary is mapped to \texttt{<unk>}, destroying
        information and making the model guess from context. For
        morphologically rich languages (Finnish, Turkish, Russian),
        the OOV rate on held-out text can exceed 30\%.
  \item \textbf{Vocabulary size.} A word-level English vocabulary that
        covers 99\% of running text requires $\sim\!250{,}000$
        entries; the embedding table dominates parameter count for
        small models, and the final softmax becomes the bottleneck
        for inference latency.
  \item \textbf{Morphology blindness.} ``run'', ``running'', ``runs'',
        and ``ran'' share no parameters — the model must learn their
        relationship from co-occurrence statistics.
  \item \textbf{Multilingual collapse.} Space-separated tokenization is
        meaningless for Chinese, Japanese, and Thai; word-level
        approaches require separate per-language segmenters.
\end{enumerate}

For these reasons, modern LLMs use subword tokenization.

\subsection{Byte-Pair Encoding (BPE)}

BPE was introduced for data compression \citep{sennrich2016bpe} and
adapted to neural machine translation; it is the tokenizer underlying
most modern LLMs.

\subsubsection{Training Algorithm}

Given a training corpus:
\begin{enumerate}
  \item \textbf{Initialize} the vocabulary as the set of base symbols
        (characters, or in byte-level BPE, the 256 bytes) plus a small
        set of special tokens.
  \item \textbf{Count adjacent pair frequencies} across all tokenized
        corpus lines.
  \item \textbf{Merge} the most frequent pair $(a, b)$ into a single
        new symbol $ab$, add it to the vocabulary, and record the
        merge rule.
  \item \textbf{Repeat} steps~2--3 until the vocabulary reaches a
        target size $V$ (typical: $32{,}000$--$200{,}000$).
\end{enumerate}
The output is (i)~a vocabulary (the set of symbols) and (ii)~an
\emph{ordered} list of merge rules. Encoding a new string applies the
merge rules in order, producing a deterministic segmentation.

\subsubsection{Worked Example}

Consider the tiny corpus
\begin{quote}\ttfamily
low low low low low\\
lowest lowest\\
newer newer newer newer newer newer\\
wider wider wider
\end{quote}
After pre-tokenization (split by whitespace) and character
decomposition with end-of-word marker \texttt{\_}:

\begin{center}
\begin{tabular}{|l|r|}
\hline
Token sequence & Count \\
\hline
\texttt{l o w \_}                  & 5 \\
\texttt{l o w e s t \_}            & 2 \\
\texttt{n e w e r \_}              & 6 \\
\texttt{w i d e r \_}              & 3 \\
\hline
\end{tabular}
\end{center}

Initial pair frequencies:
\texttt{lo}=7,
\texttt{ow}=7,
\texttt{we}=8,
\texttt{er}=9,
\texttt{r\_}=9,
\texttt{w\_}=5, \ldots.
The most frequent pair (tied) is \texttt{er} or \texttt{r\_}; breaking
ties by the lexicographic BPE convention, merge \texttt{e r}
$\to$ \texttt{er}. Recompute:

\begin{center}
\begin{tabular}{|l|r|}
\hline
Token sequence & Count \\
\hline
\texttt{l o w \_}                  & 5 \\
\texttt{l o w e s t \_}            & 2 \\
\texttt{n e w er \_}               & 6 \\
\texttt{w i d er \_}               & 3 \\
\hline
\end{tabular}
\end{center}

The next most frequent pair is \texttt{er \_} with count 9; merge to
\texttt{er\_}. Continue until $V$ is reached. After several iterations
the vocabulary contains frequent subwords (\texttt{low}, \texttt{er\_},
\texttt{er}, \texttt{new}, \ldots) plus rare characters (\texttt{i},
\texttt{s}, \texttt{t}). A new word like \texttt{slower} segments as
\texttt{s l o w er \_}, exercising both rare characters and learned
subwords: open-vocabulary by construction.

\subsubsection{Encoding Complexity}

Naive BPE encoding is $O(T \log V)$ per input of length $T$ with a
priority-queue implementation; the production variant used by
\texttt{tiktoken}~\citep{openai_tiktoken} uses a precomputed merge
priority table and runs at roughly $1$\,GB/s on a single core.

\subsection{WordPiece}

WordPiece~\citep{devlin2019bert} replaces BPE's ``most frequent pair''
criterion with a likelihood-maximization criterion. At each step it
merges the pair $(a, b)$ that maximizes
\begin{equation}
\label{eq:week3-wordpiece}
\mathrm{score}(a, b) \;=\;
\frac{\mathrm{count}(ab)}
     {\mathrm{count}(a)\,\mathrm{count}(b)}.
\end{equation}

\begin{derivation}[Why this score $\propto$ unigram likelihood gain]
\Assum\ Under a unigram model, each occurrence of token $t$
contributes $\log p(t) = \log(\mathrm{count}(t)/N)$ to the corpus
log-likelihood, with $N$ the total token count. Merging
$(a,b) \to ab$ \emph{replaces} $\mathrm{count}(ab)$ occurrences of
``$a$ followed by $b$'' (each currently contributing
$\log p(a) + \log p(b)$) with the same number of single $ab$ tokens
(each contributing $\log p(ab)$). The per-merge log-likelihood
change is
\begin{align*}
\Delta \ell &\approx \mathrm{count}(ab)\,\bigl[\log p(ab)
  - \log p(a) - \log p(b)\bigr]\\
&= \mathrm{count}(ab)\,\log
  \frac{\mathrm{count}(ab)/N}
       {(\mathrm{count}(a)/N)(\mathrm{count}(b)/N)}\\
&= \mathrm{count}(ab)\,\log\!\Bigl[N \cdot
  \underbrace{\tfrac{\mathrm{count}(ab)}
    {\mathrm{count}(a)\,\mathrm{count}(b)}}_{=\,\mathrm{score}(a,b)}\Bigr].
\end{align*}
So $\mathrm{score}(a,b)$ is the pointwise-mutual-information term
that governs the corpus log-likelihood gain per occurrence (up to
the constant $\log N$). Merges whose pair is \emph{over-represented}
relative to the product of marginals --- collocations --- are
preferred, which is exactly the intuition that WordPiece prefers
rare-on-their-own but frequent-together constituents.
\end{derivation}

Encoding uses greedy longest-prefix matching at inference time,
annotated with the \texttt{\#\#} prefix to mark intra-word
continuation (``playing'' $\to$ \texttt{play \#\#ing}).

\subsection{Unigram LM and SentencePiece}

\citet{kudo2018unigram} introduced a fundamentally different approach:
\emph{start} with a large seed vocabulary and \emph{prune} it down. The
tokenizer maintains a unigram language model over subwords,
\begin{equation}
\label{eq:week3-unigram}
p(\mathbf{x}) \;=\; \prod_{t} p(x_t),
\end{equation}
with the per-subword probabilities fit by EM.

\begin{derivation}[Unigram LM training: compact EM sketch]
Each corpus token sequence $\mathbf{y}$ (text) has many possible
segmentations $S(\mathbf{y}) = \{\mathbf{x}^{(1)}, \mathbf{x}^{(2)},
\dots\}$ under a given vocabulary $\mathcal{V}$.

\textbf{E-step (expected counts).} For each text $\mathbf{y}$ and
each candidate segmentation $\mathbf{x} \in S(\mathbf{y})$, compute
its posterior probability
\[
  q(\mathbf{x} \mid \mathbf{y}) \;=\;
  \frac{\prod_t p(x_t)}{\sum_{\mathbf{x}' \in S(\mathbf{y})}
    \prod_{t'} p(x'_{t'})}
\]
(done efficiently by forward-backward / Viterbi over the
segmentation lattice). The expected count of subword $s$ is
\[
  \E[\#s] \;=\; \sum_{\mathbf{y}} \sum_{\mathbf{x} \in S(\mathbf{y})}
    q(\mathbf{x}\mid\mathbf{y}) \cdot \#\{t : x_t = s\}.
\]

\textbf{M-step (closed-form update).} Renormalize:
$p(s) \leftarrow \E[\#s] / \sum_{s'} \E[\#s']$.

\textbf{Prune step.} After EM convergence on $\mathcal{V}$, delete
the subword $s^*$ whose removal causes the smallest drop in total
corpus log-likelihood:
\[
  s^* \;=\; \arg\min_{s \in \mathcal{V}}\;
    \Bigl(\ell_{\mathcal{V}} - \ell_{\mathcal{V}\setminus\{s\}}\Bigr).
\]
Iterate E/M/prune until $|\mathcal{V}| = V$.
\end{derivation}

Encoding uses the Viterbi algorithm to find the highest-likelihood
segmentation of the input given the pruned vocabulary.

\emph{SentencePiece}~\citep{kudo2018sentencepiece} is an open-source
implementation that supports both BPE and Unigram-LM and, importantly,
treats the entire input (including whitespace) as raw byte data by
default; whitespace is replaced with a sentinel (U+2581, ``\_'') so
that detokenization is guaranteed reversible. This reversibility is
not guaranteed by the original BPE formulation, which requires
language-specific detokenization rules.

\subsection{Comparison of Subword Tokenizers}

\begin{center}
\begin{tabular}{|p{2.5cm}|p{3.4cm}|p{3.0cm}|p{3.8cm}|}
\hline
Tokenizer & Training objective & Encoding & Used by \\
\hline
BPE              & Greedy pair frequency            & Deterministic
                   & GPT-2/3/4, Llama (byte-level) \\
WordPiece        & Likelihood score Eq.~\eqref{eq:week3-wordpiece}  & Greedy longest-match
                   & BERT, ALBERT \\
Unigram LM       & EM pruning, Eq.~\eqref{eq:week3-unigram}         & Viterbi (probabilistic)
                   & T5, Gemini, many multilingual models \\
Byte-level BPE   & BPE over bytes                   & Deterministic
                   & GPT-2 onwards, Llama~2/3 \\
\hline
\end{tabular}
\end{center}

Practical consequences: BPE and WordPiece are deterministic, which
simplifies caching; Unigram LM can return the $k$-best segmentations,
useful for data augmentation during training~\citep{kudo2018unigram}.
Byte-level BPE trades a slightly larger apparent vocabulary for robust
handling of all Unicode input without special-casing.

\subsubsection{Same sentence, different tokenizers}

\begin{workedexample}[Tokenizing ``Let's tokenize this!''\ three ways]
The twenty-character English string \texttt{Let's tokenize this!}
produces different ID sequences under different tokenizers
(ID-count figures are representative of public implementations at
standard vocabulary sizes):

\begin{center}
\small
\begin{tabular}{|l|p{6.8cm}|r|}
\hline
\textbf{Tokenizer} & \textbf{Segmentation} & \textbf{\#tokens} \\
\hline
GPT-2 byte-BPE ($V{=}50257$) &
  \texttt{[Let][\textquoteright s][ tokenize][ this][!]} & 5 \\
BERT WordPiece ($V{=}30522$) &
  \texttt{[let][\textquotesingle][s][token][\#\#ize][this][!]} & 7 \\
SentencePiece Unigram ($V{=}32000$) &
  \texttt{[\_Let][\textquoteright][s][\_token][ize][\_this][!]} & 7 \\
\hline
\end{tabular}
\end{center}

Three points to note. First, the punctuation and apostrophe are
handled differently by each scheme (attached vs.\ emitted as a
separate token), which matters for stop-sequence matching. Second,
byte-BPE attaches leading whitespace to the following token (the
``\texttt{ tokenize}'' whitespace-oddity discussed below), so
concatenating prompts naively across a whitespace boundary can
shift the segmentation. Third, on non-English scripts the counts
diverge far more dramatically (A3.3): the same sentence in Thai or
Amharic can be 2--5$\times$ more tokens under a GPT-2-era tokenizer
than under a multilingual-tuned one.
\end{workedexample}

\begin{provedassumed}[Tokenizer comparison: what is established vs assumed]
\textbf{Proved.} BPE and WordPiece are deterministic given their
merge tables (A3.1). SentencePiece's whitespace-as-sentinel design
guarantees lossless detokenization (A3.2). WordPiece's score
Eq.~\eqref{eq:week3-wordpiece} is proportional to the unigram
log-likelihood gain per merge (derivation above). Unigram-LM EM
maximizes corpus log-likelihood under the Bayesian posterior over
segmentations.\\
\textbf{Empirical.} Subword regularization (k-best sampling under
Unigram-LM) improves downstream accuracy on specific
tasks~\citep{kudo2018unigram}; byte-BPE handles all Unicode without
fallback kernels on tested corpora.\\
\textbf{Assumed.} The unigram-model \emph{segmentation} is a
useful approximation to the true joint distribution over subword
sequences --- it ignores context and treats tokens as independent
(A3.3). Real text has strong local dependence; this is why
different tokenizers can produce measurably different perplexities
on the same corpus without any change to the model.
\end{provedassumed}

\subsection{The GPT Whitespace Oddity and Other Quirks}

GPT-2's tokenizer~\citep{radford2019language,openai_tiktoken} is
byte-level BPE with one consequential detail: leading whitespace is
prepended to the following token rather than emitted separately. The
string ``\ the'' (with a leading space) and ``the'' (without) produce
different token IDs, and the model has been trained to expect one
form or the other depending on context.

This has engineering implications:
\begin{itemize}
  \item Naive prompt concatenation
        (\texttt{prompt + user\_message}) can produce whitespace
        boundaries the tokenizer segments unexpectedly, degrading
        quality.
  \item Stop sequences must be specified in post-tokenization token
        IDs, not as raw strings, or they may straddle token boundaries
        and never match.
  \item Newlines, tabs, and non-breaking spaces are separate tokens,
        and small whitespace changes can flip behavior.
\end{itemize}
Practitioners resolve these by always tokenizing the fully assembled
prompt as a single step, logging the resulting token IDs alongside the
text, and writing assertions that critical prompt prefixes segment
into a stable number of tokens.

\subsection{Embeddings as Learned Geometry}

\subsubsection{The Embedding Layer is a Linear Map}

Given a vocabulary of size $V$ and embedding dimension $d$, the
embedding layer is parameterized by a matrix $E \in \mathbb{R}^{V \times d}$.
For a token ID $i$, the input embedding is the $i$-th row:
\begin{equation}
\label{eq:week3-embed}
\mathbf{e}_i \;=\; E^{\top} \mathbf{1}_i,
\end{equation}
where $\mathbf{1}_i \in \{0,1\}^{V}$ is the one-hot indicator vector.
Mathematically, this is nothing other than a linear projection from
the $V$-dimensional one-hot simplex into $\mathbb{R}^{d}$; the
``lookup'' is an efficient implementation that skips the $V \times d$
matrix multiply. The embedding matrix is $O(Vd)$ parameters — for
$V = 10^5$ and $d = 8{,}000$, this is $8\times 10^8$ parameters, which
is often the single largest block in a model's parameter count.

\subsubsection{Tied Input and Output Embeddings}

The output layer of a language model computes unnormalized logits
\begin{equation}
\label{eq:week3-unembed}
\mathbf{z} \;=\; W_{\mathrm{out}}\,\mathbf{h},
\qquad
W_{\mathrm{out}} \in \mathbb{R}^{V \times d},
\end{equation}
where $\mathbf{h}$ is the final hidden state and $\mathbf{z}$ is fed
into the softmax of Week~1. \emph{Tying} weights sets
$W_{\mathrm{out}} = E$, so the input embedding and the output projection
share parameters. This halves the embedding parameter budget and,
empirically, improves perplexity by a small but consistent margin on
most settings. Tied embeddings are standard in encoder-decoder (T5) and
most decoder-only (GPT, Llama) architectures. The inductive bias is
that tokens with similar \emph{use} (appearing in similar contexts)
should map to similar \emph{output} probabilities, matching the
distributional hypothesis below.

\subsubsection{Positional Encoding (Preview)}

Transformer attention is permutation-equivariant; without extra
machinery the model cannot distinguish ``dog bites man'' from
``man bites dog''. Positional encodings restore order information.
Classical variants (sinusoidal, learned) are additive to the embedding;
rotary~\citep{su2021rope} and ALiBi~\citep{press2022alibi} are
multiplicative/additive inside the attention kernel. Positional
encoding is covered in depth in Week~4.

\subsection{The Distributional Hypothesis and Analogy Vectors}

\citet{mikolov2013efficient,mikolov2013distributed} operationalized the
distributional hypothesis — \emph{a word's meaning is characterized by
the company it keeps} — in the Word2Vec family of models. Given a
sliding window, Word2Vec trains an embedding $\mathbf{e}_w$ to
maximize the log-likelihood of observed co-occurrences. The
famous analogy result is
\begin{equation}
\mathbf{e}_{\text{king}} \;-\; \mathbf{e}_{\text{man}}
\;+\; \mathbf{e}_{\text{woman}}
\;\approx\; \mathbf{e}_{\text{queen}},
\end{equation}
which suggests that gender-semantic offsets are encoded as a
consistent direction in embedding space. GloVe~\citep{pennington2014glove}
refined the objective to directly target a logarithm of co-occurrence
ratios.

\emph{Cautionary notes for practitioners:}
\begin{itemize}
  \item LLM embeddings from modern models are trained jointly with the
        rest of the network, not with a Word2Vec objective; analogy
        vectors are not guaranteed to work.
  \item The analogy result is sensitive to normalization; it typically
        requires unit-norm vectors and cosine similarity.
  \item Analogies that ``work'' often reflect dataset statistics rather
        than semantic structure (see bias discussion below).
\end{itemize}

\subsection{Embedding Geometry}

\subsubsection{Cosine Similarity and Anisotropy}

Similarity between embeddings is conventionally measured by cosine:
\begin{equation}
\cos(\mathbf{e}_i, \mathbf{e}_j) \;=\;
\frac{\mathbf{e}_i \cdot \mathbf{e}_j}
     {\|\mathbf{e}_i\|\,\|\mathbf{e}_j\|}.
\end{equation}
\Emp~Modern Transformer embeddings are highly \emph{anisotropic}: almost
all token vectors concentrate in a narrow cone of the $d$-dimensional
space, so the mean cosine similarity between random tokens is
typically in the $0.5$--$0.8$ range rather than the $0$ a uniformly-distributed
embedding would predict~\citep{ethayarajh2019contextual,gao2019representation}.
These are reported empirical ranges across the models studied in
those works (BERT, GPT-2, ELMo families); newer architectures with
different training objectives or explicit isotropy regularisers
shift the ranges, and the precise numbers depend on the measurement
protocol (layer, subset of vocabulary, pre-processing).

\begin{center}
\fbox{\parbox{0.93\linewidth}{
\textbf{Caveat (preprocessing dependence).} The anisotropy numbers above,
and any downstream cosine-based retrieval quality claim, are sensitive
to pre-processing: mean-subtraction (centering), per-dimension whitening,
and norm-based filtering each change the reported cosine distribution
materially. Before citing a ``cosine similarity of $x$'' figure,
specify: (i) which layer's embeddings, (ii) whether mean-centering or
whitening was applied, and (iii) the token subset (full vocabulary vs.\
content words vs.\ a held-out sample). Without those three, cosine
numbers are not comparable across papers.
}}
\end{center}

The practical implication: raw cosine similarity is uninformative
without centering or whitening. Downstream retrieval pipelines
(Week~9) routinely apply a mean-subtraction or PCA step before
comparing embeddings, and the effectiveness of that step is itself an
empirical finding, not a theorem.

\subsubsection{Visualization: PCA and t-SNE}

Two-dimensional visualization of token embeddings is a useful but
treacherous tool. PCA preserves global linear structure; t-SNE and
UMAP preserve local neighborhood structure at the expense of global
geometry. Interpretation rules of thumb:
\begin{itemize}
  \item A t-SNE cluster \emph{probably} reflects a local similarity
        structure, but apparent distances between clusters carry
        little information.
  \item PCA directions that explain more than a few percent of
        variance correspond to coarse semantic axes
        (part-of-speech, frequency, language).
  \item Subword tokens (especially rare byte-level fragments) often
        form a separate ``cruft cluster'' far from content words;
        this is a property of training, not a bug.
\end{itemize}
For production monitoring, a better tool is a \emph{probe}: a small
linear classifier that predicts a property (e.g., language, sentiment
polarity) from the embedding. Its accuracy is an operational signal
for whether the embedding has drifted.

\subsection{Bias and Fairness in Embeddings}

Embeddings inherit the statistical structure of their training
corpus. \citet{bolukbasi2016man} showed that Word2Vec vectors
trained on Google News encode gender stereotypes
(\texttt{computer\_programmer} is closer to \texttt{man} than to
\texttt{woman}, \emph{etc.}). Similar patterns recur in modern LLM
embeddings, although now mediated by the much richer contextualized
representations of the Transformer stack (Week~4).

Three engineering positions on bias in embeddings:
\begin{itemize}
  \item \textbf{Do not debias at the embedding layer.} Doing so hides
        the problem from downstream observability.
  \item \textbf{Treat embeddings as an audit surface.} Run fairness
        probes on the embedding layer to detect training-data shifts.
  \item \textbf{Mitigate at the application layer.} Prompt design,
        output classifiers, and human review are typically more
        effective than post-hoc embedding projections.
\end{itemize}
This is reinforced in Week~8 (alignment) and Week~10 (governance).

\subsection{Systems Engineering Implications}

\begin{center}
\begin{tabular}{|p{3.4cm}|p{3.4cm}|p{6.8cm}|}
\hline
V-model stage & Tokenizer / embedding property & System requirement or activity \\
\hline
Requirements & Tokenizer is part of the API contract
             & Version-pin alongside weights; publish hash in model
               card. \\
 & Multilingual fairness & Budget token-per-character ratios by
   language (see \S\,Multilingual below). \\
\hline
Architecture & $E$ dominates parameter count at small scale
             & Share $E$ with $W_{\mathrm{out}}$ (tied embeddings). \\
 & Byte-level fallback & Prefer byte-level BPE for
   robustness to user input. \\
\hline
Implementation & Whitespace / newline handling
               & Tokenize fully-assembled prompts, not fragments. \\
 & Normalization form & Pin NFC (or NFKC) at data ingest. \\
\hline
V\&V & Tokenization invariants & Golden-file tests for canonical
   strings; hash-compare outputs across versions. \\
 & Embedding drift & Probe-based monitoring, not
   cosine-of-random-pairs. \\
\hline
Operations & Tokenizer versions in logs
           & Record tokenizer hash per request; alert on mismatch. \\
 & Vocabulary drift & Reject inputs with excessive
   \texttt{<unk>} or bytes-tokens rate. \\
\hline
\end{tabular}
\end{center}

\subsection{Multilingual and Billing Considerations}

\Emp~A BPE vocabulary trained on an English-heavy corpus allocates most of
its merge rules to English patterns. On other languages the same
vocabulary segments less aggressively, producing typically $2$--$5\times$
as many tokens per Unicode code-point for non-Latin scripts such as
Thai, Amharic, or Burmese, relative to English under the same
tokenizer~\citep{ahia2023languages,petrov2023language}. The $2$--$5\times$
figure is an observation range across the tokenizers and language pairs
studied in those references; individual (language, tokenizer) pairs
fall outside this range in either direction, and frontier-model
tokenizers from 2024 onward narrow the gap materially. Treat the range
as a planning anchor, not a guarantee. The practical effects:
\begin{itemize}
  \item \textbf{Billing disparity}: per-token-priced APIs charge more
        for the same information content in Thai, Amharic, or Burmese
        than in English. This is a recognized fairness issue and
        some frontier tokenizers (e.g., Claude's, Gemini's) widen
        multilingual coverage explicitly.
  \item \textbf{Context-window deflation}: a 32k-token window covers
        far fewer characters in non-dominant languages.
  \item \textbf{Code behavior}: tokenizers optimized for prose may
        tokenize source code into inefficient fragments; some
        tokenizers (GPT-4o, Llama~3) re-train with a code-heavy mix.
\end{itemize}
When building multilingual applications, measure the token-to-codepoint
ratio for each target language and plan context budgets accordingly.

\subsection{Human Factors and Failure Modes}

\begin{itemize}
  \item \textbf{Assuming token = word}. Users who reason in words are
        frequently surprised by token counts, cost, and latency.
  \item \textbf{Prompt brittleness}. Small whitespace or punctuation
        changes can shift the segmentation, sometimes producing
        qualitatively different outputs.
  \item \textbf{``The model doesn't know $X$''}. Sometimes the model
        knows $X$ but cannot produce it because the relevant subword
        is rare or absent in the vocabulary (e.g., uncommon API
        identifiers are sometimes splatted across multiple tokens).
  \item \textbf{Overreading analogies}. Embeddings encode statistical
        regularities from training; analogy vectors are not symbolic
        reasoning and should not be treated as ground truth.
  \item \textbf{Unicode trickery and prompt-injection}. Homoglyphs and
        invisible code-points can be used adversarially to smuggle
        instructions into a prompt. Normalize and log at ingress.
\end{itemize}

\subsection{Bridges to Later Chapters}

\begin{itemize}
  \item \textbf{Week 4 (Transformer Architecture)}: the embedding
        matrix $E$ feeds directly into the residual stream; positional
        encodings (RoPE, ALiBi) are layered on top.
  \item \textbf{Week 5 (Pretraining \& In-Context)}: the effective
        dataset size $D$ in the Chinchilla law from Week~2 is measured
        in \emph{tokens}; a different tokenizer produces a different
        training compute accounting.
  \item \textbf{Week 6 (Efficiency, Scaling, MoE)}: tied embeddings
        and byte-level vocabularies keep $V$ manageable; MoE gating
        operates on post-embedding activations.
  \item \textbf{Week 7 (Adaptation, LoRA, Quantization)}: embedding
        tables are quantization hot-spots because of their outsized
        parameter share; LoRA adaptation typically freezes them.
  \item \textbf{Week 9 (LLM Systems: RAG, Tools)}: retrieval uses
        sentence- or document-level embeddings distinct from token
        embeddings; anisotropy correction matters for cosine-based
        retrieval.
  \item \textbf{Week 10 (Governance \& Assurance)}: tokenizer and
        embedding provenance are artefacts in a model card /
        datasheet~\citep{mitchell2019modelcards,gebru2021datasheets}.
\end{itemize}

\subsection{Key References}

\begin{itemize}
  \item \citet{sennrich2016bpe} --- Byte-Pair Encoding for neural MT.
  \item \citet{kudo2018sentencepiece} --- SentencePiece implementation.
  \item \citet{kudo2018unigram} --- Unigram-LM subword regularization.
  \item \citet{devlin2019bert} --- BERT and WordPiece.
  \item \citet{openai_tiktoken} --- \texttt{tiktoken} reference
        implementation.
  \item \citet{mikolov2013efficient,mikolov2013distributed} ---
        Word2Vec and the distributional hypothesis.
  \item \citet{pennington2014glove} --- GloVe embeddings.
  \item \citet{bolukbasi2016man} --- gender bias in word embeddings.
  \item \citet{su2021rope}, \citet{press2022alibi} --- modern
        positional encoding schemes (previewed here, developed in
        Week~4).
\end{itemize}

\subsection{Recommended University Lectures}

\begin{itemize}
  \item Stanford CS224N --- word vectors, embeddings, and subword
        models, \citet{stanford_cs224n}.
  \item Stanford CS324 --- Large Language Models, tokenization
        module, \citet{stanford_cs324}.
  \item MIT NLP group resources, \citet{mit_nlp}.
\end{itemize}

\subsection{Practitioner Lab}

\begin{enumerate}
  \item \textbf{Train a BPE tokenizer}. Using the Hugging Face
        \texttt{tokenizers} library or a from-scratch implementation,
        train a $32{,}000$-merge BPE on a 10\,MB text sample. Compare
        against the same corpus with a $16{,}000$-merge and a
        $64{,}000$-merge vocabulary.
  \item \textbf{Reverse-engineer GPT tokenization}. Use
        \texttt{tiktoken} to tokenize the strings \texttt{"the"},
        \texttt{" the"}, \texttt{"The"}, \texttt{"\textbackslash nthe"}.
        Tabulate the resulting token IDs and document which strings
        surprise you.
  \item \textbf{Language fairness audit}. Tokenize parallel
        translations of the UDHR into ten languages with
        \texttt{tiktoken}. Plot tokens-per-character by language.
  \item \textbf{Analogy test}. On a pretrained model of your choice,
        extract the input embeddings for \{king, queen, man, woman\}
        and compute $\|E_{\text{king}} - E_{\text{man}} +
        E_{\text{woman}} - E_{\text{queen}}\|$. Compare with a random
        quadruple.
  \item \textbf{Anisotropy check}. Sample 10{,}000 random pairs of
        token embeddings and plot the cosine-similarity histogram.
        Overlay the histogram after mean-centering.
  \item \textbf{Tokenizer versioning}. Write a regression test that
        fails whenever a canonical prompt segments into a different
        token-ID sequence across tokenizer versions. Integrate it
        into a CI pipeline.
\end{enumerate}

\subsection*{Derivation Ledger (Ch. 3)}

\begin{derivledger}
Compact index of the chapter's central identities.
\begin{itemize}
  \item \textbf{Tokenization pipeline.}\
        Bytes $\to$ pre-tokens $\to$ subword sequence
        --- Eq.~\eqref{eq:week3-pipeline}; deterministic under
        \textbf{(A3.1)}.
  \item \textbf{WordPiece score = PMI.}\
        $\mathrm{score}(a,b) = \log\frac{P(ab)}{P(a)P(b)}$
        --- Eq.~\eqref{eq:week3-wordpiece}; the score is the
        per-merge unigram log-likelihood gain.
  \item \textbf{Unigram-LM EM-prune.}\
        E-step: posterior over segmentations;
        M-step: $p(s) \leftarrow \mathbb{E}[\#s] / \sum_{s'} \mathbb{E}[\#s']$;
        prune $s^* = \arg\min_s (\ell_V - \ell_{V \setminus \{s\}})$
        --- Eq.~\eqref{eq:week3-unigram}.
  \item \textbf{Embedding lookup.}\
        $e_i = E_{\iota(w_i),:}$ --- Eq.~\eqref{eq:week3-embed}.
  \item \textbf{Unembedding (weight tying).}\
        Logit $z_v = e^\top E_v$ --- Eq.~\eqref{eq:week3-unembed}.
\end{itemize}
\end{derivledger}

\subsection{Week 3 Takeaway}

\begin{quote}
Tokenization defines the alphabet the model will ever perceive, and
embeddings define the geometry in which it reasons. Both are
engineered artifacts — versioned, cached, audit-able — and both carry
statistical commitments that propagate through every downstream
stage of the system.
\end{quote}
