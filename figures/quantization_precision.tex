\begin{tikzpicture}[
  fmt/.style={draw, rounded corners, minimum width=2.6cm, minimum height=1.0cm,
              align=center, font=\footnotesize},
  arrow/.style={->, thick, >=Stealth},
  node distance=0.4cm and 0.5cm
]

\node[fmt, fill=black!5] (fp32) {\textbf{FP32}\\32 bits\\range $\sim 10^{\pm 38}$};
\node[fmt, right=of fp32, fill=black!5] (fp16) {\textbf{FP16}\\16 bits\\range $\sim 10^{\pm 5}$};
\node[fmt, right=of fp16, fill=black!5] (bf16) {\textbf{BF16}\\16 bits\\range $\sim 10^{\pm 38}$};
\node[fmt, below=0.5cm of fp32, fill=orange!15] (int8) {\textbf{INT8}\\8 bits\\$[-128,127]$};
\node[fmt, right=of int8, fill=orange!15] (fp8) {\textbf{FP8 (E4M3)}\\8 bits\\$\sim 10^{\pm 2}$};
\node[fmt, right=of fp8, fill=orange!30] (int4) {\textbf{INT4 / NF4}\\4 bits\\16 levels};

\draw[arrow] (fp32) -- (fp16);
\draw[arrow] (fp32) -- (bf16);
\draw[arrow] (fp16) -- (int8);
\draw[arrow] (bf16) -- (fp8);
\draw[arrow] (fp8)  -- (int4);
\draw[arrow] (int8) -- (int4);

\node[font=\footnotesize\itshape, below=0.4cm of int8, xshift=3.8cm, align=center, text width=9cm]
  {training $\leftarrow$ \hspace{1cm} more range / precision \hspace{1cm} $\rightarrow$ inference\\fewer bits $=$ less HBM, higher arithmetic intensity, more quantization error};

\end{tikzpicture}
