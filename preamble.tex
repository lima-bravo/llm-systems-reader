%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% BASIC PACKAGES
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage[margin=1in]{geometry}
\usepackage{setspace}
\onehalfspacing

\usepackage[T1]{fontenc}
\usepackage{lmodern}

\usepackage{microtype}
\usepackage{booktabs}
\usepackage{array}
\usepackage{multirow}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% MATH AND SYMBOLS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsfonts}
\usepackage{bm}

\newcommand{\E}{\mathbb{E}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\KL}{D_{\mathrm{KL}}}
\newcommand{\entropy}{\mathcal{H}}

% Unit macros (entropy / cross-entropy)
\newcommand{\nats}{\mathrm{nats}}
\newcommand{\bits}{\mathrm{bits}}

% Variance operator
\providecommand{\Var}{\mathrm{Var}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% CLAIM-TYPE LABELS (REVIEW_PLAN.md E.3)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Short \paragraph-level labels that mark the epistemic status of the
% paragraph that follows. Usage: \Def\ body text ...
\newcommand{\Def}{\paragraph{Definition.}}
\newcommand{\Ident}{\paragraph{Identity.}}
\newcommand{\Assum}{\paragraph{Assumption.}}
\newcommand{\Emp}{\paragraph{Empirical Observation.}}
\newcommand{\Cons}{\paragraph{Engineering Consequence.}}
\newcommand{\Heur}{\paragraph{Engineering Heuristic.}}
\newcommand{\Norm}{\paragraph{Normative Directive.}}
\newcommand{\Find}{\paragraph{Deployment Finding.}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% STRUCTURED BLOCKS (REVIEW_PLAN.md E.1, E.2, E.4, E.5)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Opening "Notation and Assumptions" block for every chapter.
\newenvironment{notationbox}
  {\par\medskip\noindent\textbf{Notation and Assumptions.}\par\nobreak\noindent\ignorespaces}
  {\par\medskip}

% Stepwise "Derivation" block; wrap an align* inside.
\newenvironment{derivation}[1][]
  {\par\medskip\noindent\textbf{Derivation (Stepwise)#1.}\par\nobreak\noindent\ignorespaces}
  {\par\medskip}

% Worked numeric example block.
\newenvironment{workedexample}[1][]
  {\par\medskip\noindent\textbf{Worked Example#1.}\par\nobreak\noindent\ignorespaces}
  {\par\medskip}

% End-of-section "Proved vs Assumed" recap block.
\newenvironment{provedassumed}
  {\par\medskip\noindent\textbf{Proved vs Assumed (Recap).}\par\nobreak\noindent\ignorespaces}
  {\par\medskip}

% End-of-chapter "Derivation Ledger" appendix block (REVIEW_PLAN.md P2 item 10).
% Compact list of key identities tied to their equation labels. Keep short
% (bulleted list of one-liners, each pointing to a labelled equation).
\newenvironment{derivledger}[1][]
  {\par\medskip\noindent\textbf{Derivation Ledger#1.}\par\nobreak\noindent\ignorespaces}
  {\par\medskip}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% CHAPTER OPENER — "SETTING THE SCENE" DESIGN (Option B)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage[framemethod=default]{mdframed}

% Small uppercase sans-serif label replacing \section*{Setting the Scene}.
% Sits directly below the chapter title rule, no blank line before prose.
\newcommand{\scenelabel}{%
  \par\vspace{4pt}%
  \noindent{\sffamily\footnotesize\MakeUppercase{Setting the scene}}%
  \par\nobreak\vspace{2pt}%
}

% Left-bordered orientation strip: lists the chapter's opening topics
% so the reader knows what the "Setting the Scene" section covers
% before the prose begins.
%
% Usage:
%   \begin{scenestrip}
%     \sceneitem{The problem}
%     \sceneitem{How we got here}
%     \sceneitem{Where we stand}
%     \sceneitem{What goes wrong}
%   \end{scenestrip}
\newmdenv[
  topline=false, bottomline=false, rightline=false,
  linewidth=1.5pt, linecolor=black!25,
  innerleftmargin=10pt, innerrightmargin=0pt,
  innertopmargin=3pt, innerbottommargin=3pt,
  skipabove=4pt, skipbelow=10pt
]{scenestrip}

% One navigation entry inside the strip.
\newcommand{\sceneitem}[1]{\noindent{\small\sffamily #1}\par\vspace{1pt}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% FIGURES AND DIAGRAMS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage{graphicx}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}

% TikZ for diagrams (see figures/*.tex)
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, fit, calc, backgrounds}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% CODE AND VERBATIM
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage{listings}
\usepackage{xcolor}

\definecolor{codegray}{rgb}{0.95,0.95,0.95}

\lstset{
  backgroundcolor=\color{codegray},
  basicstyle=\ttfamily\small,
  frame=single,
  breaklines=true,
  showstringspaces=false
}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% REFERENCES AND LINKS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage{natbib}
\usepackage{url}
\usepackage{hyperref}

\hypersetup{
  colorlinks=true,
  linkcolor=black,
  citecolor=black,
  urlcolor=blue
}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% CHAPTER AND SECTION STYLING (book class)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage{titlesec}

% Chapter heading: number on its own line, title below, ruled separator
\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries}
  {\chaptertitlename\ \thechapter}{16pt}{\Huge}
\titlespacing*{\chapter}{0pt}{30pt}{20pt}

\titleformat{\section}
  {\normalfont\Large\bfseries}
  {\thesection}{1em}{}

\titleformat{\subsection}
  {\normalfont\large\bfseries}
  {\thesubsection}{1em}{}

\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries}
  {\thesubsubsection}{1em}{}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% TABLES FOR SYSTEMS / FMEA STYLE ANALYSIS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\newcolumntype{R}[1]{>{\raggedleft\arraybackslash}p{#1}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% MISC
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\setlength{\parskip}{0.5em}
\setlength{\parindent}{0pt}

